name: Prima Matrix

on:
  workflow_dispatch:


jobs:
  first-matrix:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest,windows-latest]

    steps:
      - name: Saluto
        run: echo "Ciao da ${{ matrix.os}}"